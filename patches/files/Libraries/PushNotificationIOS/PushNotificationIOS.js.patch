--- /Users/sethagent/Developer/react-native-upstream/packages/react-native/Libraries/PushNotificationIOS/PushNotificationIOS.js	2026-03-06 15:51:54
+++ /Users/sethagent/Developer/react-native-macos-fork/packages/react-native/Libraries/PushNotificationIOS/PushNotificationIOS.js	2026-03-06 15:22:52
@@ -67,7 +67,9 @@
   new NativeEventEmitter<NativePushNotificationIOSEventDefinitions>(
     // T88715063: NativeEventEmitter only used this parameter on iOS. Now it uses it on all platforms, so this code was modified automatically to preserve its behavior
     // If you want to use the native module on other platforms, please remove this condition and test its behavior
-    Platform.OS !== 'ios' ? null : NativePushNotificationManagerIOS,
+    Platform.OS !== 'ios' && Platform.OS !== 'macos' // [macOS] Also use this parameter on macOS
+      ? null
+      : NativePushNotificationManagerIOS,
   );
 
 const _notifHandlers = new Map<string, void | EventSubscription>();
@@ -210,7 +212,7 @@
       NativePushNotificationManagerIOS,
       'PushNotificationManager is not available.',
     );
-    // $FlowFixMe[incompatible-type]
+    // $FlowFixMe[prop-missing]
     NativePushNotificationManagerIOS.presentLocalNotification(details);
   }
 
@@ -235,7 +237,7 @@
       NativePushNotificationManagerIOS,
       'PushNotificationManager is not available.',
     );
-    // $FlowFixMe[incompatible-type]
+    // $FlowFixMe[prop-missing]
     NativePushNotificationManagerIOS.scheduleLocalNotification(details);
   }
 
@@ -580,7 +582,8 @@
     if (
       !this._isRemote ||
       !this._notificationId ||
-      this._remoteNotificationCompleteCallbackCalled
+      this._remoteNotificationCompleteCallbackCalled ||
+      Platform.OS === 'macos' // [macOS]
     ) {
       return;
     }
