--- /Users/sethagent/Developer/react-native-upstream/packages/react-native/Libraries/WebSocket/WebSocket.js	2026-03-06 18:50:06
+++ /var/folders/jp/b_h7tjf10rg_htfhxp6tds100000gn/T/tmp.o6mDmN91la	2026-03-06 18:50:46
@@ -141,8 +141,10 @@
     this._eventEmitter = new NativeEventEmitter(
       // T88715063: NativeEventEmitter only used this parameter on iOS. Now it uses it on all platforms, so this code was modified automatically to preserve its behavior
       // If you want to use the native module on other platforms, please remove this condition and test its behavior
-      Platform.OS !== 'ios' ? null : NativeWebSocketModule,
-    );
+      Platform.OS !== 'ios' && Platform.OS !== 'macos' // [macOS]
+        ? null
+        : NativeWebSocketModule,
+    );
     this._socketId = nextWebSocketId++;
     this._registerEvents();
     NativeWebSocketModule.connect(url, protocols, {headers}, this._socketId);
